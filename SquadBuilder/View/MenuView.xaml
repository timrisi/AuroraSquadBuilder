<?xml version="1.0" encoding="UTF-8"?>
<mvvm:BaseView 	xmlns="http://xamarin.com/schemas/2014/forms" 
				xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
				xmlns:mvvm="clr-namespace:XLabs.Forms.Mvvm;assembly=XLabs.Forms"
				x:Class="SquadBuilder.MenuView"
				Title="Menu" 
				Icon="@drawable/icon" >
	<mvvm:BaseView.Content>
		<StackLayout Orientation="Vertical"
					 Padding="5,25" 
					 BackgroundColor="Silver"
					 Spacing="5" >
			<Button Text="Squadrons" HorizontalOptions="FillAndExpand" Command="{Binding ShowSquadrons}" />
			<Button Text="Custom Factions" HorizontalOptions="FillAndExpand" Command="{Binding ShowCustomFactions}" />
			<Button Text="Custom Ships" HorizontalOptions="FillAndExpand" Command="{Binding ShowCustomShips}" />
			<Button Text="Custom Pilots" HorizontalOptions="FillAndExpand" Command="{Binding ShowCustomPilots}" />
			<Button Text="Custom Upgrades" HorizontalOptions="FillAndExpand" Command="{Binding ShowCustomUpgrades}" />
			<Button Text="Settings" HorizontalOptions="FillAndExpand" Command="{Binding ShowSettings}" />
			<Button Text="Feedback" HorizontalOptions="FillAndExpand" Command="{Binding SendFeedback}" />
		</StackLayout>
	</mvvm:BaseView.Content>
</mvvm:BaseView>
