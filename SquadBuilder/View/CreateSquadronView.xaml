<?xml version="1.0" encoding="UTF-8"?>
<mvvm:BaseView xmlns="http://xamarin.com/schemas/2014/forms" 
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
			 xmlns:mvvm="clr-namespace:XLabs.Forms.Mvvm;assembly=XLabs.Forms"
			 x:Class="SquadBuilder.CreateSquadronView"
			 Title="{Binding SquadName}">
	<mvvm:BaseView.Content>
		<StackLayout Padding="15,10,15,10">
			<Entry Text="{Binding SquadName, Mode=TwoWay}" Placeholder="{Binding PlaceholderText}" />
			<Entry Text="{Binding Points}" />
			<Picker x:Name="factionPicker" SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}" Title="Faction" />
			<Button Text="{Binding SaveButtonText}" Command="{Binding SaveSquadron}" />
		</StackLayout>
	</mvvm:BaseView.Content>
</mvvm:BaseView>
